create keyspace INFORMATION_ITEMS;
use INFORMATION_ITEMS;

create column family META with comparator = UTF8Type;
create column family META_INDEX with comparator = TimeUUIDType;

create column family PARENTS with comparator = TimeUUIDType;
create column family OLDIES with comparator = TimeUUIDType;

create column family COMPONENTS with comparator = TimeUUIDType;
create column family DIRECT_2 with comparator = TimeUUIDType and column_type = 'Super' and subcomparator = TimeUUIDType;
create column family DIRECT_3 with comparator = TimeUUIDType and column_type = 'Super' and subcomparator = TimeUUIDType;

create column family INDIRECT with comparator = TimeUUIDType;

create column family CACHE_TYPE_MOVIE with comparator = TimeUUIDType;
